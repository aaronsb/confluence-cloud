name: Build .mcpb release artifact

on:
  push:
    tags: ['v*']

jobs:
  build-mcpb:
    name: Build .mcpb bundle
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Install mcpb CLI
        run: npm install -g @anthropic-ai/mcpb@^2

      - name: Build .mcpb bundle
        run: make mcpb

      - name: Create GitHub Release (if not exists)
        run: |
          TAG="${GITHUB_REF_NAME}"
          gh release view "$TAG" 2>/dev/null || gh release create "$TAG" --title "$TAG" --generate-notes
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload .mcpb to release
        run: |
          TAG="${GITHUB_REF_NAME}"
          gh release upload "$TAG" confluence-cloud-mcp.mcpb --clobber
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
